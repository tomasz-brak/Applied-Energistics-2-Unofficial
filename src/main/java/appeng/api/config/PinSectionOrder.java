package appeng.api.config;

/**
 * Which pin section is displayed first in the terminal (player pins vs crafting auto-add pins).
 */
public enum PinSectionOrder {

    PLAYER_FIRST,
    CRAFTING_FIRST;
}
